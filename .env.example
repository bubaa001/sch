# Example .env for Gmail SMTP (DO NOT commit real secrets)
# Copy this to .env and fill in your real values.

SECRET_KEY=your-secure-secret-key

# Gmail SMTP settings (requires 2FA + App Password)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# Optional: explicitly suppress sending emails even if MAIL_SERVER is set
# Set to 'true' to disable sending in development
MAIL_SUPPRESS_SEND=false
